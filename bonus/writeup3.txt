when starting the machine we can rapidly spam the key {ESC} to access the boot menu (grub)
so we see:

boot:

pressing {TAB} we se the partitions:

boot:
 live

now we can initiate the partition with bash:

boot: live init=/bin/bash
error: unexpectedly disconnected from boot status daemon
...
...
root@BornToSecHackMe:/#
